# mipt-build-systems
Example project of how to work with CMake.

## Requirements
* `cmake >= 3.10`
* `python`
* `doxygen`

## Build
1. `mkdir build`
2. `cd build`
3. `cmake ..`
4. `make`

## Run
`./BuildSystemsFun`
